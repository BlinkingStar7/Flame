#include <bits/stdc++.h>
using namespace std;

int main() {
	scanf("%d", &N);
	for (int i=0; i<N; ++i)
		scanf("%d", &A[i]);

	scanf("%d", &M);
	for (int i=0; i<M; ++i) {
		int t;
		scanf("%d", &t);
		
